<!--
UPLOAD SHEET - do not paste this comment into Modrinth.

  File            fabric/build/libs/meshelium-1.6.0.jar
                  (built artifacts stay untracked; this sheet does not)
  Sources jar     None. The build does not produce one - the LGPL source
                  obligation is met by the public-repo copy below, and no
                  published version has ever carried a sources jar.
  Public source   Publish this build's code to github.com/ded811/Meshelium,
                  by hand as always. That copy IS the LGPL obligation for
                  this build, which is why no sources jar is uploaded. Do it
                  at upload time rather than after: the obligation starts
                  when the binary is distributed.

                  USE THE SCRIPT, NOT A COPY:
                      python tools/prepare-public.py <out> --compare <public>
                  Copying this repo wholesale also copies its .gitignore over
                  the public one, which is the only thing holding back
                  docs/unreleased/ and 19 internal design docs - 53 files.
                  The script builds the tree, writes the right .gitignore and
                  refuses if anything held back slipped in. Verified
                  2026-09-19: the tree it produces builds both jars on its
                  own, so nothing the build needs is being held back.

                  NEW FOR 1.6.0 AND EASY TO MISS: the public copy has no
                  `sodium/` directory. That whole source set postdates
                  beta.1 and IS compiled into this jar (unzip -l shows
                  com/deds/meshelium/sodium/**), so it is corresponding
                  source and must go with the push. Nothing in the public
                  .gitignore excludes it, and settings.gradle needs no
                  change - both loaders pull it in via fabric/build.gradle.
                  README.md, CHANGELOG.md and docs/MODRINTH.md over there
                  are still the pre-Sodium text and must be updated in the
                  same push, or the public repo will describe a mod that
                  does not work with Sodium while the jar beside it does.

                  ALSO CHECK BEFORE PUSHING: the public remote had moved
                  ahead of the local public working copy when this sheet was
                  written (remote main 61a2ed4c, local 93d8029). Reconcile
                  before attempting a push; never --force.
  Version number  1.6.0
  Version name    1.6.0 - Fabric
  Release channel RELEASE
  Loaders         Fabric            (do NOT tick NeoForge on this version)
  Game versions   26.2
  Dependencies    Fabric API - required
                  Sodium     - optional   (tested against 0.9.2-beta.1)
                  Bobby      - optional   (suggested in fabric.mod.json)

  Sodium is genuinely optional: the mod runs without it, and the adapter
  mixins are gated by MesheliumSodiumMixinPlugin on Sodium's classes being
  visible. Ticking it as an optional dependency is the only place a player
  actually sees the relationship, since neither manifest declares it.

  This is an UPDATE to the existing Fabric line. Players are coming from
  1.5.x or from 1.6.0 beta 1, so the notes lead with what changed, not with
  what the mod is.

  Everything below the line is the version changelog. It covers 1.6.0 beta 1
  to 1.6.0 - the betas in between were never published.
-->

**Meshelium works with Sodium now - and together they are a lot faster.**

They used to be a choice: one replaced the other. Now you get both. Sodium
keeps building the chunks, which is what it is best at, and Meshelium draws
them with mesh shaders.

Measured on a Radeon RX 9070 XT at 1920x1080, standing in the same spot in the
same world: with Meshelium drawing, the frame is about **twice as fast as
Sodium on its own** - 1.9x at render distance 64, and 1.8x at 96.

Install both and it happens on its own; there is nothing to switch on. It works
on Fabric and on NeoForge.

Use **Sodium 0.9.2-beta.1**. Modrinth lists it under Versions as a beta, so you
have to pick it deliberately rather than taking the top entry. The Meshelium
settings screen shows which version you have and says whether it is the one
this build was made for.

**New setting: GPU Visibility.**

On by default, and only when Sodium is installed. Meshelium works out what you
can actually see on the graphics card itself, instead of drawing everything it
is handed. That is what used to make the frame rate dip while you looked
around.

**The settings screen is shorter with Sodium installed.**

The settings that only change how Meshelium builds chunks have nothing to do
while Sodium is doing that job, so they are hidden. They come back if you
remove Sodium. The rest of the menu is shorter and plainer throughout.

**Known: render distance 120 needs more memory than Minecraft usually has.**

The Distance Cap lets you choose 120, but at that distance Minecraft runs out
of Java heap and closes. That is Minecraft's own memory rather than
Meshelium's. Raise the cap gradually, and give Minecraft more memory in your
launcher before pushing it far.
