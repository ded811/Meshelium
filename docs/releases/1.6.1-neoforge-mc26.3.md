<!--
UPLOAD SHEET - do not paste this comment into Modrinth.

  File            neoforge/build/libs/meshelium-neoforge-1.6.1+mc26.3.jar
                  (built with: ./gradlew build)
  Sources jar     None. The build does not produce one - the LGPL source
                  obligation is met by the public-repo copy below, and no
                  published version has ever carried a sources jar.
  Public source   Publish this build's code to github.com/ded811/Meshelium,
                  by hand as always. That copy IS the LGPL obligation for
                  this build. Do it at upload time rather than after: the
                  obligation starts when the binary is distributed.

                  USE THE SCRIPT, NOT A COPY:
                      python tools/prepare-public.py <out> --compare <public>
                  NEW FOR 1.6.1: the public copy has no `versions/` directory
                  yet. It is corresponding source (every jar is built from
                  it) and the script publishes it by default; nothing in the
                  public .gitignore excludes it. README, CHANGELOG and
                  docs/MODRINTH.md over there must go in the same push.

  Version number  1.6.1+mc26.3
  Version name    1.6.1 - NeoForge - Minecraft 26.3
  Release channel RELEASE
  Loaders         NeoForge          (do NOT tick Fabric on this version)
  Game versions   26.3              (ONLY 26.3 - the other game version is a separate file)
  Dependencies
                  Sodium     - optional   (tested against 0.9.2+mc26.3, a release)

  NeoForge note: this jar does NOT contain Sodium. The build unwraps
  Sodium's nested jar at COMPILE time only (extractSodiumNeoForge in
  neoforge/build.gradle) and the shipped jar carries no class under
  net/caffeinemc and no nested jars. Sodium is PolyForm Shield and is
  never redistributed.

  Four files this time, one per loader per game version. Upload each with
  its own sheet; the changelog body below is the same on all four.

  VERIFIED (2026-09-20, RX 9070 XT, driver 26.8.1; re-run in full after the
  launch check landed, jars built 18:54):
    - full Fabric client suite on 26.3, Vulkan, terrain, rd48, under the
      Vulkan validation layer: green, 29 screenshots read, 0 validation
      messages
    - Sodium stand-down suite on 26.3 with Sodium 0.9.2+mc26.3, validation
      layer on: green, 50 screenshots, GPU-visibility rung owned 8873 of
      8877 attempts, 0 validation messages
    - the same two suites on 26.2 (the 26.2 jar): green, 29 + 50
      screenshots, rung owned 8932 of 8936, 0 validation messages
    - NeoForge proof runs into a world on 26.3 and 26.2, each with and
      without Sodium: four screenshots read
    - launch check: "fine" on every 26.3 run (the harness passes the flag),
      warns and toasts at StackShadowPages=31 (screenshot read), silent on
      26.2 runs even at 8
  LAUNCHER NOTE (2026-09-20): a fresh MultiMC 0.7.0 instance of this build
  died silently (exit 0xC0000005) at start-up / title screen / Options -
  with the mod REMOVED too. Cause: MultiMC (and Prism 11.1) drop Mojang's
  26.3 launch-profile JVM args; -XX:StackShadowPages=32 alone fixes it
  (6/6 vs 0/2), and lowering it in the dev client reproduces the death.
  Not a Meshelium bug. This build adds MesheliumLaunchCheck (log WARN +
  title-screen toast when the flag is missing, 26.3 builds only);
  docs/TROUBLESHOOTING.md carries the fix, linked from the changelog.

  Everything below the line is the version changelog.
-->


**Minecraft 26.3.**

Meshelium now runs on Minecraft 26.3 as well as 26.2, on Fabric and on
NeoForge. Download the file for your game version; they are all the same
mod, and everything below applies to both.

Minecraft 26.3 changed how it draws the world - terrain and everything else
now share one render pass, and "Improved Transparency" became a new
order-independent method - and Meshelium follows both without you doing
anything. With Improved Transparency on, Meshelium draws the water and glass
itself and Minecraft's own transparency pass is skipped for terrain, so the
picture is the same as with the setting off.

**NeoForge on 26.3: no loading-screen workaround needed.** NeoForge for
26.3 currently ships with its early loading screen switched off (its
loader ignores the setting), so the Vulkan start-up problem Meshelium works
around on 26.2 cannot happen there. Meshelium leaves it alone and says so in
the log. On 26.2 the workaround stays exactly as it was.

**If your launcher skips a Java setting, Meshelium now tells you.**

Mojang's launcher passes `-XX:StackShadowPages=32` to every Minecraft 26.3
launch. MultiMC leaves it out (Prism Launcher fixed this through its
metadata in September 2026), and without it the game can close without a
word, mods or not. On 26.3, Meshelium checks for it when the game starts
and, if it is missing, says so in the log and with a toast on the title
screen, with the argument to add. Nothing changes when your launcher
already passes it, and nothing changes on 26.2, which does not need it.
Details: <https://github.com/ded811/Meshelium/blob/main/docs/TROUBLESHOOTING.md>

**Sodium 0.9.2 is a release now, on both game versions.**

Use Sodium 0.9.2 for your game version - the release, not the newer 0.9.3
alpha. On 26.2, Sodium 0.9.2-beta.1 is the same build with a different
version number, and Meshelium treats it as the same Sodium instead of
warning about it.

**Fixed: a memory-copy the graphics driver was not asked permission for.**

When Meshelium's terrain records grew for the first time, they were copied
out of a buffer that had not been created for copying. AMD's driver allowed
it; the Vulkan rules do not, and a stricter driver could have refused. Both
game versions have the fix.
