<!--
UPLOAD SHEET - do not paste this comment into Modrinth.

  File            neoforge/build/libs/meshelium-neoforge-1.6.0.jar
                  (built artifacts stay untracked; this sheet does not)
  Sources jar     None. The build does not produce one - the LGPL source
                  obligation is met by the public-repo copy below, and no
                  published version has ever carried a sources jar.
  Public source   Publish this build's code to github.com/ded811/Meshelium,
                  by hand as always. That copy IS the LGPL obligation for
                  this build. Do it at upload time rather than after: the
                  obligation starts when the binary is distributed.

                  USE THE SCRIPT, NOT A COPY:
                      python tools/prepare-public.py <out> --compare <public>
                  Copying this repo wholesale also copies its .gitignore over
                  the public one, which is the only thing holding back
                  docs/unreleased/ and 19 internal design docs - 53 files.
                  The script builds the tree, writes the right .gitignore and
                  refuses if anything held back slipped in. Verified
                  2026-09-19: the tree it produces builds both jars on its
                  own, so nothing the build needs is being held back.

                  NEW FOR 1.6.0 AND EASY TO MISS: the public copy has no
                  `sodium/` directory. That whole source set postdates
                  beta.1 and IS compiled into this jar (unzip -l shows
                  com/deds/meshelium/sodium/**), so it is corresponding
                  source and must go with the push. README.md, CHANGELOG.md
                  and docs/MODRINTH.md over there are still the pre-Sodium
                  text and must be updated in the same push.

                  ALSO CHECK BEFORE PUSHING: the public remote had moved
                  ahead of the local public working copy when this sheet was
                  written (remote main 61a2ed4c, local 93d8029). Reconcile
                  before attempting a push; never --force.
  Version number  1.6.0
  Version name    1.6.0 - NeoForge
  Release channel RELEASE
  Loaders         NeoForge          (do NOT tick Fabric on this version)
  Game versions   26.2
  Dependencies    Sodium - optional   (tested against 0.9.2-beta.1)
                  (NeoForge builds need no Fabric API equivalent. Do not add
                  Fabric API here - it would be wrong and confusing.)

  NeoForge note: this jar does NOT contain Sodium. The build unwraps
  Sodium's nested jar at COMPILE time only (extractSodiumNeoForge in
  neoforge/build.gradle); unzip -l on the shipped jar shows zero entries
  under net/caffeinemc and no nested jars. Sodium is PolyForm Shield and is
  not redistributed.

  The Fabric build is a separate upload with its own sheet. Ship them
  together: the notes below mention both loaders.

  Everything below the line is the version changelog. It covers 1.6.0 beta 1
  to 1.6.0 - the betas in between were never published.
-->

**Meshelium works with Sodium now.**

That is the headline of this release. Sodium builds the chunks, exactly as
it always has, and Meshelium draws them with mesh shaders. Install both and
it happens on its own - there is nothing to switch on.

Also here: two fixes for problems that only showed up at long render
distances, and the settings screen no longer buries you in text.

---

## New: Meshelium works with Sodium

They used to be a choice: one replaced the other. Now they work together, on
Fabric and on NeoForge.

Use **Sodium 0.9.2-beta.1**. Modrinth lists it under Versions as a beta, so
you have to pick it deliberately rather than taking the top entry. Meshelium
plugs into parts of Sodium that were never meant for other mods to touch, so
another version may not fit; the Meshelium settings screen shows which one
you have and says whether it matches. If anything looks wrong, start the
game with `-Dmeshelium.sodium.adapter=false` and Sodium goes back to drawing
everything itself.

With Sodium installed the Meshelium settings screen is shorter. The settings
that only change how Meshelium builds chunks have nothing to do while Sodium
is doing that job, so they are hidden. They come back if you remove Sodium.

## New setting: GPU Visibility

On by default, and only when Sodium is installed. Meshelium works out what
you can actually see on the graphics card itself instead of drawing
everything Sodium hands it. Sodium's list grows a lot whenever you move the
camera, which is what used to make the frame rate dip while you looked
around.

## Fixed: the game could freeze for good at high render distances, with Sodium

At render distance 96, turning the camera for a while could lock the picture
up completely. It was not a slow frame and not your graphics driver
crashing: the card was handed one drawing command that had been overwritten
with nonsense, and it sat there working on it until Minecraft gave up
waiting. Meshelium now makes the buffer involved large enough that the
situation cannot arise.

## Fixed: terrain went missing at render distance 96 and above

A separate limit, found while chasing the first one. A table of per-frame
drawing work was sized against a test world that turned out to be a quarter
of the size everyone believed. At render distance 64 it fit. At 96 it ran
out, and when it ran out it quietly stopped drawing some of the ground.

The table is now sized from your Distance Cap. If your cap is 32 or 48
nothing changes at all; higher caps get a bigger table, in graphics memory
rather than system memory.

## Fixed: with Sodium installed, the "switch to Vulkan" offer never appeared

The one group it mattered most to. Switching to Vulkan is exactly what turns
the mesh-shader path on for a Sodium player, and they were the only ones
never asked.

## Fixed: the "Meshelium is off" message never arrived if you skipped the main menu

Beta 1 fixed the mod itself in that case. The message explaining why it was
staying off still waited for a main menu you were never going to see. It now
arrives wherever you are, once, and waits if you are in a menu, or if
something is hitting you.

## Also in this release

The settings screen tells you which Sodium you have and whether it is the
one this build was made for. The menu text is a good deal shorter and
plainer throughout. Meshelium does a little less work per frame with Sodium
installed.

## Known: render distance 120 needs more memory than Minecraft usually has

The Distance Cap lets you choose 120, but at that distance Minecraft runs
out of Java heap and closes. This is not specific to Meshelium - it is
Minecraft's own memory - but the setting offers a number the game cannot
always reach. Raise the cap gradually, and give Minecraft more memory in
your launcher before pushing it far.
