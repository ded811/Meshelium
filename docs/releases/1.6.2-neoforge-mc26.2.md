<!--
UPLOAD SHEET - do not paste this comment into Modrinth.

  File            neoforge/build/libs/meshelium-neoforge-1.6.2+mc26.2.jar
                  (built with: ./gradlew build -Pmc=26.2)
  Sources jar     None. The build does not produce one - the LGPL source
                  obligation is met by the public-repo copy below, and no
                  published version has ever carried a sources jar.
  Public source   Publish this build's code to github.com/ded811/Meshelium,
                  by hand as always. That copy IS the LGPL obligation for
                  this build. Do it at upload time rather than after: the
                  obligation starts when the binary is distributed.

                  USE THE SCRIPT, NOT A COPY:
                      python tools/prepare-public.py <out> --compare <public>
                  The public copy must carry `versions/` (it arrived with
                  1.6.1; if 1.6.1 was never pushed, it goes in this push).
                  It is corresponding source (every jar is built from it)
                  and the script publishes it by default. README, CHANGELOG
                  and docs/MODRINTH.md over there must go in the same push.

  Version number  1.6.2+mc26.2
  Version name    1.6.2 - NeoForge - Minecraft 26.2
  Release channel RELEASE
  Loaders         NeoForge          (do NOT tick Fabric on this version)
  Game versions   26.2              (ONLY 26.2 - the other game version is a separate file)
  Dependencies
                  Sodium     - optional   (tested against 0.9.2+mc26.2, a release)

  NeoForge note: this jar does NOT contain Sodium. The build unwraps
  Sodium's nested jar at COMPILE time only (extractSodiumNeoForge in
  neoforge/build.gradle) and the shipped jar carries no class under
  net/caffeinemc and no nested jars. Sodium is PolyForm Shield and is
  never redistributed.

  Four files this time, one per loader per game version. Upload each with
  its own sheet; the changelog body below is the same on all four.

  IF 1.6.1 WAS NEVER UPLOADED TO MODRINTH: paste CHANGELOG.md's 1.6.1
  section (Minecraft 26.3, the launcher check, Sodium 0.9.2, the copy fix)
  under the body below, so the version's notes cover the jump from 1.6.0.

  VERIFIED (2026-09-24, RX 9070 XT, AMD driver 26.8.1, jars built 01:15 and
  staged in release/1.6.2/; the flash fix on the owner's laptop: Radeon 780M,
  RADV Mesa 26.2.2, Kubuntu 26, Wayland):
    - THE FIX, on the machine that showed it: two owner runs (Fabric 26.3,
      Sodium 0.9.2, flying and spamming F3+A), no flashing with default
      settings nor with the group hold on. The GPU path drew the whole
      session: 0 refused chunk groups, 0 stale record reads, no fallback.
      Those runs used the 2026-09-23 22:38 build; everything changed after
      it (the region-hold fix, the four review fixes, two rows dropped, the
      rename) is off the default drawing path. The release jar itself on
      the laptop, 2026-09-24 01:03-01:09 (flying, F3+A, render distance 32
      and 63): log clean, 0 refused chunk groups, 0 stale record reads, no
      fallback; the record store grew 1024 -> 1536 at render distance 63
      and the fast path resumed the next frame
    - THE SAME LAPTOP ON WINDOWS (AMD driver 25.10.30.18, 2026-09-24): 1.6.1
      flashed there too. Release jar, defaults: 0 refused chunk groups, 0
      stale record reads, no fallback. Release jar with the old behaviour
      forced back on (mirrorHostCoherent=false, commitInDrawBuffer=false,
      fallbackOnStale=false): 81,312 refused chunk groups and visible
      flashing. So the fault is the integrated chip under both drivers, not
      Linux, and the fix covers Windows laptops as well
    - Sodium stand-down suite on the release build, 26.3, Vulkan,
      validation layer on: green, 14 legs, 52 screenshots read (both
      settings screens show GPU Occlusion Culling and Repair Chunk Records
      only), 0 validation messages, 0 refusals, no fallback. One picture
      pair read 0.24% (97_18 wall): all 1004 differing pixels are the HUD
      hunger icons wobbling, the terrain is pixel-identical
    - the same suite on 26.2 (rename included): green, 52 screenshots, 0
      validation messages, 0 refusals
    - full Fabric suite, 26.3 and 26.2, Vulkan, terrain, rd48, validation
      layer on: green, 28 screenshots each read, 0 validation messages
      (both after the rows were dropped; the rename is text on a row these
      suites never build)
    - Sodium suite in its OpenGL form (10 shots: the adapter-only rows
      struck through) and with the adapter declined (8 shots): green
    - the Sodium suite with -Dmeshelium.sodium.regionHold=true: green, 15
      groups held in the session, one frame each
    - NeoForge proof runs into a world, 26.3 and 26.2, each with and
      without Sodium: four screenshots read, terrain complete; the Sodium
      runs log the GPU path and the commit in the draws' command buffer
    - speed on a discrete card (the owner asked): this build against the
      last commit, forest-rd64 at 2560x1440 with Sodium, still and moving
      camera, alternated in one session with same-build repeats as the
      control: no measurable difference (frame +0.3% still, +0.9% moving
      on the mean with -1.3% on the median, GPU passes within 1%; two runs
      of the same build differ by up to 2.6%). MEASUREMENTS.md section 0u
    - two read-only reviews: one of the flash-fix diff (4 real minor
      findings, all fixed before the runs above) and one of the gap
      detector (the region hold was sticky; fixed)
    - the whole story: docs/unreleased/sodium/FLASH-INVESTIGATION.md 2i-2m
  Everything below the line is the version changelog.
-->


**Meshelium's settings now live inside Sodium's video settings.**

With Sodium installed, Meshelium has its own entry in Sodium's video
settings, in the list on the left under Sodium. It has all the settings you
had before, and Sodium's Apply and Undo buttons work on them. The
"Meshelium..." row at the bottom of the Options screen is gone. A
**Meshelium Settings...** button on the new page opens Meshelium's own
screen for the things a list of settings can't show: what Meshelium is
doing right now, which Sodium version you have, the Enable Vulkan button,
and Reset. Without Sodium nothing changes: the Meshelium Settings button at
the top of Video Settings is where it always was, and `/meshelium` works
everywhere.

**Fixed: big squares of terrain flashing with GPU Visibility on.** With
Sodium installed and the GPU Visibility (Sodium) setting on, whole squares
of the world could blink out for a moment while chunks were loading, worst
right after pressing F3+A. It happened on AMD's built-in graphics, the
kind in many laptops: we tested a Radeon 780M on both Windows and Linux,
and it's fixed on both. If you still see it on your computer, please
report it. And if Meshelium ever catches your
graphics card drawing from out-of-date chunk data again, it switches to a
slower, safe way of drawing until you restart Minecraft. At most a frame or
two can flash before it does.

**Two new settings under Advanced**, on Meshelium's page in Sodium's video
settings (they only appear with Sodium installed). Both are on by default.
Leave them on unless you're asked to turn one off while reporting a
problem.

- **GPU Occlusion Culling:** only does anything while GPU Visibility
  (Sodium) is on. Your graphics card skips terrain hidden behind hills,
  mountains or anything else solid. It adds to the culling Sodium already
  does on its own and doesn't change any Sodium setting.
- **Repair Chunk Records:** if your graphics card ever refuses to draw part
  of the world because its copy of Meshelium's chunk data is out of date,
  Meshelium sends it a fresh copy. With this update's fix it shouldn't ever
  need to.

**Fixed: render distance jumping back to 12.** With Sodium installed, a
saved render distance higher than Sodium's slider allows is now lowered to
the slider's highest value when you open Sodium's settings, instead of
jumping back to 12.
